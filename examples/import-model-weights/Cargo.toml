[package]
authors = ["Dilshod Tadjibaev (@antimora)"]
edition.workspace = true
license = "MIT OR Apache-2.0"
name = "import-model-weights"
publish = false
version.workspace = true

[lints]
workspace = true

[dependencies]

cortex = { path = "../../crates/cortex", features = [
    "ndarray",
    "dataset",
    "vision",
] }

cortex-store = { path = "../../crates/cortex-store", features = [
    "std",
    "pytorch",
    "safetensors",
    "burnpack",
], default-features = false }
