[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science"]
description = "Automatic differentiation backend for the Cortex framework"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "data"]
license.workspace = true
name = "cortex-autodiff"
readme.workspace = true
repository = "https://github.com/qora-protocol/cortex/tree/main/crates/cortex-autodiff"
documentation = "https://docs.rs/cortex-autodiff"
version.workspace = true

[lints]
workspace = true

[features]
default = ["std", "tracing"]
std = ["dep:parking_lot"]
export_tests = []         # check checkpointer is_empty in tests

tracing = [
    "dep:tracing",
    "cortex-std/tracing",
    "cortex-backend/tracing",
]

[dependencies]
cortex-std = { path = "../cortex-std", version = "=0.21.0-pre.1", default-features = false }
cortex-backend = { path = "../cortex-backend", version = "=0.21.0-pre.1", default-features = false }


derive-new = { workspace = true }
spin = { workspace = true }
parking_lot = { workspace = true, optional = true }
log = { workspace = true }
hashbrown = { workspace = true }
num-traits = { workspace = true }
portable-atomic = { workspace = true }
tracing = { workspace = true, optional = true, features = ["default"] }


[package.metadata.docs.rs]
features = ["default"]
rustdoc-args = ["--cfg", "docsrs"]
