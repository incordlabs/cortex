[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science", "no-std", "embedded", "wasm"]
description = "Tensor library with user-friendly APIs and automatic differentiation support"
documentation = "https://docs.rs/cortex-tensor"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "tensor", "pytorch", "ndarray"]
license.workspace = true
name = "cortex-tensor"
readme.workspace = true
repository = "https://github.com/qora-protocol/cortex/tree/main/crates/cortex-tensor"
version.workspace = true

[lints]
workspace = true

[features]
default = ["std"]
doc = ["default"]
std = [
    "num-traits/std",
    "cortex-std/std",
    "cortex-backend/std",
    "colored",
]
tracing = [
    "cortex-std/tracing",
    "cortex-backend/tracing",
]

cubecl = ["cortex-std/cubecl", "cortex-backend/cubecl"]
cubecl-cuda = ["cortex-backend/cubecl-cuda"]
cubecl-hip = ["cortex-backend/cubecl-hip"]
cubecl-wgpu = ["cortex-backend/cubecl-wgpu"]
cubecl-cpu = ["cortex-backend/cubecl-cpu"]
experimental-named-tensor = []

[dependencies]
cortex-std = { path = "../cortex-std", version = "=0.21.0-pre.1", default-features = false }
cortex-backend = { path = "../cortex-backend", version = "=0.21.0-pre.1", default-features = false }

colored = { workspace = true, optional = true }
derive-new = { workspace = true }
num-traits = { workspace = true }

# Device
hashbrown = { workspace = true }
spin = { workspace = true }
thiserror = { workspace = true }

# Serialization
serde = { workspace = true }

[target.'cfg(not(target_has_atomic = "ptr"))'.dependencies]
portable-atomic-util = { workspace = true }

[dev-dependencies]
serial_test = { workspace = true }

[package.metadata.docs.rs]
features = ["doc"]
rustdoc-args = ["--cfg", "docsrs", "--html-in-header", "katex-header.html"]
